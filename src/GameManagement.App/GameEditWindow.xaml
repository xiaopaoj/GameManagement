<Window x:Class="GameManagement.GameEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="编辑游戏名称与备注" Height="330" Width="620" MinHeight="300" MinWidth="520"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="22">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <TextBlock Text="游戏名称" FontWeight="SemiBold"/>
        <TextBox x:Name="NameText" Grid.Row="1" Padding="8" Margin="0,6,0,14"/>
        <TextBlock Grid.Row="2" Text="备注（允许留空）" FontWeight="SemiBold"/>
        <TextBox x:Name="NoteText" Grid.Row="3" Padding="8" Margin="0,6,0,14" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" IsCancel="True" Padding="20,8" Margin="4"/>
            <Button Content="保存" IsDefault="True" Click="Save_Click" Padding="20,8" Margin="4"/>
        </StackPanel>
    </Grid>
</Window>
